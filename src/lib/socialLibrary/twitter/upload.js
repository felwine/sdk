
export default async ({
  sourceUrl,
  filename,
  auth,
  // contentType = 'image/png'
}) => {

  try {
    const {

    } = auth

    return { url }

  } catch (e) {
    console.error(e)
  }
  return null
}
